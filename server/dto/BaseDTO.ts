// Bu sınıf tüm DTO'lar için temel sınıfı tanımlar.
// Girdi: -
// Çıktı: BaseDTO sınıfı
// Hata: -
export abstract class BaseDTO {
  // DTO'lar genelde versiyon bilgisi içerebilir
  version?: string
}
